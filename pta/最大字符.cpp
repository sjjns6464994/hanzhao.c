#include <stdio.h>
#include <string.h>

int main()
{
	char s[10000], str[10000][10000], temp, s1[1000][1000];
	int i, j, k, l, count = 0, max = 0;
	fgets(s, sizeof(s), stdin);
	s[strcspn(s, "\n")] = 0;
	for (i = 0; i < strlen(s); i++)
	{
		
	}
}

